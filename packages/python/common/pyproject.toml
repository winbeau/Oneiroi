[project]
name = "oneiroi-common"
version = "0.1.0"
description = "Shared contracts for Oneiroi Studio services"
requires-python = ">=3.12,<3.14"
dependencies = [
    "pydantic>=2.10.6,<3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
